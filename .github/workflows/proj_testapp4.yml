# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - testapp4

on:
  push:
    branches:
      - proj
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://mcr.microsoft.com/
        username: ${{ secrets.AzureAppService_ContainerUsername_0c47b50366c747609e37b5f1915444e0 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_a788b854c52a475ab6ca9663ee9dc041 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: mcr.microsoft.com/${{ secrets.AzureAppService_ContainerUsername_0c47b50366c747609e37b5f1915444e0 }}/hipoint/app:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'testapp4'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_0d29aac045cc4d5387e9186b48e5cdd3 }}
        images: 'mcr.microsoft.com/${{ secrets.AzureAppService_ContainerUsername_0c47b50366c747609e37b5f1915444e0 }}/hipoint/app:${{ github.sha }}'